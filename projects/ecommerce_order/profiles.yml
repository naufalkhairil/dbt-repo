ecommerce_order:
  outputs:
    bronze:
      dbname: postgres
      host: localhost
      pass: postgres
      port: 5432
      schema: public
      threads: 1
      type: postgres
      user: postgres
    silver:
      dbname: postgres
      host: localhost
      pass: postgres
      port: 5432
      schema: silver
      threads: 1
      type: postgres
      user: postgres
    gold:
      dbname: postgres
      host: localhost
      pass: postgres
      port: 5432
      schema: gold
      threads: 1
      type: postgres
      user: postgres
    gold_duckdb__order:
      type: duckdb
      path: /home/pal/ICS/github/dbt-repo/databases/duckdb/workspace/db/ecommerce/order.duckdb
      schema: main
      attach:
        - path: postgresql://postgres:postgres@localhost:5432/postgres
          alias: postgres
          type: postgres

  target: gold_duckdb__order